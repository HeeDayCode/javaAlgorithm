package programmers.level1;

public class Average {
	//합을 구하고
    for(int i =0 ; i < arr.length ; i++){
        answer += arr[i];
    }
    //개수로 나누기
    answer = answer/arr.length;


    return answer;
}
